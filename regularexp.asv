t=dir;
g = t(3:1002,1);
expr = '[bsd][bgrw][abc][abcdefghijklmnopqrstuvwxyz]3[n].align';
for i=1:10
    w=g(i).name;
    s=regexp(w,expr);
    if isempty(s)
        disp(i);
    else
      disp(w); 
    end
end